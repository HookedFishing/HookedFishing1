HOOKED — RAILWAY LAUNCH CHECKLIST
==================================

1. GitHub repo (already pushed):
   https://github.com/HookedFishing/HookedFishing1

2. Railway: New Project → Deploy from GitHub → HookedFishing1

3. Service → Variables (copy values from your local server\.env):

   SOLANA_RPC=https://api.mainnet-beta.solana.com
   TOKEN_MINT=G5w7X3X1zPMyCu5y5rr1qYV175iFkFVyZAy9pDLepump
   TOKEN_DECIMALS=6
   TREASURY_PRIVATE_KEY=(from local server\.env)
   JWT_SECRET=(from local server\.env — must NOT be the dev default)
   WALLET_ENCRYPTION_KEY=(from local server\.env — 32+ characters)

   Do NOT set PORT — Railway assigns it automatically.
   NODE_ENV=production is set by the Docker image.

4. Settings → Networking → Generate Domain

5. OPTIONAL but recommended — persist accounts across redeploys:
   Volumes → Add Volume → mount path: /app/server/data

6. After deploy, verify:
   https://YOUR-DOMAIN.railway.app/api/health
   Should return ok:true, treasuryConfigured:true

7. Before $HOOKED is live on Solana:
   - Guest play and account signup work (balance saved server-side)
   - On-chain rewards auto-enable when the mint appears on-chain
   - Withdrawals stay disabled in the UI until rewards are live

Treasury wallet (public): 7bw6Jx8AycbgZvmKJaB9pMhpUSUtfDf8GAPChUfM1Ha9
Token mint: G5w7X3X1zPMyCu5y5rr1qYV175iFkFVyZAy9pDLepump
